apiVersion: v1
kind: Service
metadata:
  name: skycloak-mcp
  namespace: skycloak-mcp
  labels:
    app.kubernetes.io/name: skycloak-mcp
    app.kubernetes.io/instance: skycloak-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: skycloak-mcp
    app.kubernetes.io/instance: skycloak-mcp
  ports:
    - name: http
      port: 8080
      targetPort: 8080
